# 📋 خطة: AlgoTrade Pro — إصلاحات وتطوير

**التاريخ:** 2026-05-26
**الحالة:** ✅ المرحلة 1 مكتملة

---

## 🎯 الهدف
إصلاح الأخطاء الحرجة في الكود ثم التطوير المستمر

---

## 📊 المراحل

### ✅ المرحلة 1: إصلاح الأخطاء الحرجة — مكتملة
- [x] حذف `SYMBOLS.forEach` — كان يسبب `ReferenceError` فور تحميل الصفحة
- [x] إعادة كتابة `runCycle()` — كانت تستخدم `SYMBOLS` و`brokerSymbolMap` غير موجودَين
- [x] إصلاح `const sym` في loop — تعارض في الإعلان
- [x] تحديث `formatPrice()` — كشف ديناميكي بدل IDs ثابتة
- [x] إضافة تحقق في `toggleBot()` — تحذير إذا لم تُختر أزواج
- [x] تصحيح `setStatus()` — إزالة `error` class عند قطع الاتصال العادي

### ⏳ المرحلة 2: تحسينات UX — لم تبدأ
- [ ] إضافة Stop Loss / Take Profit للصفقات
- [ ] عرض الصفقات المفتوحة في الجدول
- [ ] إضافة خيار timeframe لكل زوج بشكل مستقل
- [ ] تحسين أداء renderMiniCharts

### ⏳ المرحلة 3: موثوقية الاتصال — لم تبدأ
- [ ] Retry logic عند فشل fetchPrice
- [ ] إعادة الاتصال تلقائياً عند انقطاع الشبكة
- [ ] Health check دوري للتحقق من حالة الحساب

---

## 🔗 آخر تحديث
المرحلة 1 مكتملة — الفروع: fix/critical-bugs → main
